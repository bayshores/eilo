#!/bin/sh
# Invoke the native Hermes CLI with project-owned state.
set -eu
EILO_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
. "$EILO_ROOT/scripts/lib/runtime.sh"
eilo_initialize_profile
cd "$EILO_ROOT/.state/workspace"
exec "$EILO_PYTHON" -m hermes_cli.main "$@"
